# 🤖 TRON Transaction Tracker Bot

**TRON Transaction Tracker Bot** — это асинхронный Telegram-бот, предназначенный для мониторинга в реальном времени транзакций и изменений ресурсов (Energy/Bandwidth) на указанных TRON-адресах. Бот использует API TronGrid для получения актуальных данных и обеспечивает высокую надежность и масштабируемость благодаря асинхронной архитектуре.

## ✨ Основные возможности

* **Мониторинг в Реальном Времени**: Отслеживание входящих и исходящих транзакций с уведомлением пользователя.
* **Поддержка Активов**: Уведомления о переводах **TRX** и **USDT (TRC-20)**.
* **Делегирование Ресурсов**: Отслеживание **делегаций Энергии и Bandwidth** (входящих и исходящих).
* **Контроль QPS**: Встроенный механизм контроля частоты запросов (QPS) к TronGrid API, с поддержкой ротации нескольких ключей для повышения надежности.
* **Гибкие Настройки**: Пользователи могут включать/отключать отслеживание каждого типа транзакций (Energy, BW, TRX, USDT).
* **Админ-Функции**: Специальная кнопка **"🚬 Синхронизация"** для администраторов, позволяющая выполнить ручной `PRAGMA wal_checkpoint(TRUNCATE)` и сбросить размер WAL-файла SQLite.

## ⚙️ Технологии

| Компонент | Технология | Описание |
| :--- | :--- | :--- |
| **Язык** | Python 3.9+ | Основной язык реализации. |
| **Фреймворк** | `python-telegram-bot` | Работа с Telegram API. |
| **Асинхронность** | `asyncio`, `httpx` | Обеспечение высокой конкурентности и производительности. |
| **Планировщик** | `AsyncIOScheduler` (APScheduler) | Запуск периодических циклов проверки TRONGRID. |
| **База данных** | `aiosqlite` (SQLite + WAL) | Асинхронная, надежная и быстрая база данных для хранения адресов и ключей. |
| **API** | TronGrid, Tronscan API | Получение данных о блоках, транзакциях и подписантах. |

## 🚀 Запуск проекта (Docker)

Проект разработан для развертывания в контейнере Docker.

1.  **Настройка переменных окружения**:
    * Создайте файл `.env` на основе предоставленного `.env.extension`.
    * Укажите `TELEGRAM_BOT_TOKEN`, `ADMIN_CHAT_IDS`, и ваши TronGrid ключи.

2.  **Сборка и запуск контейнеров** (предполагая использование `docker-compose`):
    ```bash
    docker-compose up --build -d
    ```

3.  **Использование**:
    * Отправьте команду `/start` боту в Telegram, чтобы начать настройку.
    * Добавьте свой TRON-адрес и, при необходимости, свой TronGrid API ключ.
